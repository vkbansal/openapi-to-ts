/* eslint-disable */
// This code is autogenerated using @oa2ts/cli.
// Please do not modify this code directly.
import { useMutation } from '@tanstack/react-query';

export interface UsePullsSubmitReviewMutationPathParams {
	owner: string;
	repo: string;
	pull_number: number;
	review_id: number;
}

export interface UsePullsSubmitReviewMutationRequestBody {
	/**
	 * The body text of the pull request review
	 */
	body?: string;
	/**
	 * The review action you want to perform. The review actions include: `APPROVE`, `REQUEST_CHANGES`, or `COMMENT`. When you leave this blank, the API returns _HTTP 422 (Unrecognizable entity)_ and sets the review action state to `PENDING`, which means you will need to re-submit the pull request review using a review action.
	 */
	event: 'APPROVE' | 'COMMENT' | 'REQUEST_CHANGES';
}

export interface UsePullsSubmitReviewMutationProps extends UsePullsSubmitReviewMutationPathParams {
	body: UsePullsSubmitReviewMutationRequestBody;
}

/**
 *
 */

export function usePullsSubmitReviewMutation(props: UsePullsSubmitReviewMutationProps) {
	return useMutation(() => Promise.resolve());
}
