/* eslint-disable */
// This code is autogenerated using @oa2ts/cli.
// Please do not modify this code directly.
import { useMutation } from '@tanstack/react-query';

export interface UseReposTestPushWebhookMutationPathParams {
	owner: string;
	repo: string;
	hook_id: number;
}

export interface UseReposTestPushWebhookMutationProps
	extends UseReposTestPushWebhookMutationPathParams {}

/**
 * This will trigger the hook with the latest push to the current repository if the hook is subscribed to `push` events. If the hook is not subscribed to `push` events, the server will respond with 204 but no test POST will be generated.
 *
 * **Note**: Previously `/repos/:owner/:repo/hooks/:hook_id/test`
 */

export function useReposTestPushWebhookMutation(props: UseReposTestPushWebhookMutationProps) {
	return useMutation(() => Promise.resolve());
}
