/* eslint-disable */
// This code is autogenerated using @oa2ts/cli.
// Please do not modify this code directly.
import { useQuery } from '@tanstack/react-query';

export interface UseReposListReleasesQueryPathParams {
	owner: string;
	repo: string;
}

export interface UseReposListReleasesQueryQueryParams {
	/**
	 * @default 30
	 */
	per_page?: number;
	/**
	 * @default 1
	 */
	page?: number;
}

export interface UseReposListReleasesQueryProps extends UseReposListReleasesQueryPathParams {
	queryParams: UseReposListReleasesQueryQueryParams;
}

/**
 * This returns a list of releases, which does not include regular Git tags that have not been associated with a release. To get a list of Git tags, use the [Repository Tags API](https://docs.github.com/rest/reference/repos#list-repository-tags).
 *
 * Information about published releases are available to everyone. Only users with push access will receive listings for draft releases.
 */

export function useReposListReleasesQuery(props: UseReposListReleasesQueryProps) {
	const { owner, repo, queryParams } = props;

	return useQuery(['repos/list-releases', owner, repo, queryParams]);
}
