/* eslint-disable */
// This code is autogenerated using @oa2ts/cli.
// Please do not modify this code directly.
import { useMutation } from '@tanstack/react-query';

export interface UseReposTransferMutationPathParams {
	owner: string;
	repo: string;
}

export interface UseReposTransferMutationRequestBody {
	/**
	 * The username or organization name the repository will be transferred to.
	 */
	new_owner: string;
	/**
	 * ID of the team or teams to add to the repository. Teams can only be added to organization-owned repositories.
	 */
	team_ids?: number[];
}

export interface UseReposTransferMutationProps extends UseReposTransferMutationPathParams {
	body: UseReposTransferMutationRequestBody;
}

/**
 * A transfer request will need to be accepted by the new owner when transferring a personal repository to another user. The response will contain the original `owner`, and the transfer will continue asynchronously. For more details on the requirements to transfer personal and organization-owned repositories, see [about repository transfers](https://help.github.com/articles/about-repository-transfers/).
 */

export function useReposTransferMutation(props: UseReposTransferMutationProps) {
	return useMutation(() => Promise.resolve());
}
