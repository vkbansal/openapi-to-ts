/* eslint-disable */
// This code is autogenerated using @oa2ts/cli.
// Please do not modify this code directly.

import { fetcher as _fetcher } from './fetcher';

export interface PackagesRestorePackageForAuthenticatedUserPathParams {
	package_type: 'container' | 'docker' | 'maven' | 'npm' | 'nuget' | 'rubygems';
	package_name: string;
}

export interface PackagesRestorePackageForAuthenticatedUserQueryParams {
	token?: string;
}

export interface PackagesRestorePackageForAuthenticatedUserProps
	extends Omit<RequestInit, 'method' | 'body'>,
		PackagesRestorePackageForAuthenticatedUserPathParams {
	queryParams: PackagesRestorePackageForAuthenticatedUserQueryParams;
	paramsSerializer?: (params: PackagesRestorePackageForAuthenticatedUserQueryParams) => string;
}

/**
 * Restores a package owned by the authenticated user.
 *
 * You can restore a deleted package under the following conditions:
 *   - The package was deleted within the last 30 days.
 *   - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first.
 *
 * To use this endpoint, you must authenticate using an access token with the `packages:read` and `packages:write` scope. If `package_type` is not `container`, your token must also include the `repo` scope.
 */
export async function packagesRestorePackageForAuthenticatedUser(
	props: PackagesRestorePackageForAuthenticatedUserProps,
) {
	const { package_type, package_name, ...rest } = props;

	return _fetcher<unknown, PackagesRestorePackageForAuthenticatedUserQueryParams, unknown>({
		path: `/user/packages/${package_type}/${package_name}/restore`,
		method: 'POST',
		...rest,
	});
}
