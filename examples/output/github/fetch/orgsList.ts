/* eslint-disable */
// This code is autogenerated using @oa2ts/cli.
// Please do not modify this code directly.

import { fetcher as _fetcher } from './fetcher';

export interface OrgsListQueryParams {
	since?: number;
	/**
	 * @default 30
	 */
	per_page?: number;
}

export interface OrgsListProps extends Omit<RequestInit, 'method' | 'body'> {
	queryParams: OrgsListQueryParams;
	paramsSerializer?: (params: OrgsListQueryParams) => string;
}

/**
 * Lists all organizations, in the order that they were created on GitHub.
 *
 * **Note:** Pagination is powered exclusively by the `since` parameter. Use the [Link header](https://docs.github.com/rest/overview/resources-in-the-rest-api#link-header) to get the URL for the next page of organizations.
 */
export async function orgsList(props: OrgsListProps) {
	const { ...rest } = props;

	return _fetcher<unknown, OrgsListQueryParams, unknown>({
		path: `/organizations`,
		method: 'GET',
		...rest,
	});
}
